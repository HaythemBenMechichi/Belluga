<form method="post" action="{{ path('ban', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to ban this user ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('ban' ~ user.id) }}">
    <button class="btn">BAN</button>
</form>