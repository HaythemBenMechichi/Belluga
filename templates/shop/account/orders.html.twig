{% extends 'menu.html.twig' %}
{% block title %}Commandes{% endblock title %}

{% block shop_content %}

<header class="mb-4">
    <h1>Mes commandes</h1>
</header>

<nav>
    <ul class="nav nav-tabs user-account-tabs">
        <li class="nav-item">
            <a href="{{ path('user_account') }}" class="nav-link active">Compte</a>
        </li>
        <li class="nav-item">
            <a href="{{ path('user_orders') }}" class="nav-link active">Commandes</a>
        </li>
    </ul>
</nav>

<div class="card">
    <div class="card-body">
        {% if orders is empty %}
            <p>Pas de commande pour l'instant.</p>
        {% else %}
            <table class="table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Date</th>
                        <th>Statut</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    {% for order in orders %}
                        <tr>
                            <td>
                                {{ order.id }}
                            </td>
                            <td>
                                {{ order.dateCreated|date() }}
                            </td>
                            <td>
                                <div class="badge badge-danger">
                                    {{ order.status }}
                                </div>
                            </td>
                            <td>
                                {{ order.transaction.total }} DT
                            </td>
                             <td>
        <a href="{{ path('user_order', { 'id': order.id }) }}" class="btn btn-info">details</a>

                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
    {{ knp_pagination_render(orders) | raw}}

        {% endif %}
    </div>
</div>

{% endblock %}
